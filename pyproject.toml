[project]
name = "metadata-driven-kappa-lakehouse"
version = "0.7.0"
description = "Strict layered metadata-driven Kappa Lakehouse prototype"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
